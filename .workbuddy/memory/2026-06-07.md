# 2026-06-07 — 项目清理：去重、修复死链、删除废弃文件

## 1. 产品页去重
- 删除17个旧格式 `.md` 文件（`products/snap-in/cd*.md` + `products/screw/cd*.md`）
- 保留 `cdxxx/index.md` 版本，避免 Jekyll 导航重复

## 2. 应用领域去重
- 删除 `ups-systems.md`（保留内容更丰富的 `ups.md`）
- 删除 `welding-equipment.md`（保留内容更丰富的 `welder.md`）
- `inverter.md` 和 `solar-inverter.md` 内容不同，均保留

## 3. 修复死链
- `applications/index.md`：ups-systems → ups，welding-equipment → welder
- `index.md`：7处旧路径 `/applications/by-industry/*` → `/applications/*`

## 4. 删除废弃文件
- `_layouts/product.html`：无任何页面引用（layout: product）
- `_includes/image_placeholder.html`：旧模板残留
- `_html_preview/`：过期的预览构建文件

## 5. 配置更新
- `.gitignore` 添加 `__pycache__/`

## 6. CSS 清理
- 删除 `.product-image-placeholder` 规则（废弃的图片占位符样式）
- 保留 `.product-header`/`.product-content`（仍被17个产品详情页使用）

## 清理统计
| 类别 | 操作 | 数量 |
|:---|---:|---:|
| 旧产品 MD | 删除 | 17 |
| 重复应用文章 | 删除 | 2 |
| 废弃模板/Include | 删除 | 2 |
| 过期预览文件 | 删除 | ~45 |
| 死链修复 | 更新 | 8 |
