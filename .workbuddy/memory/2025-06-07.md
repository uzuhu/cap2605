## 2025-06-07 上午（最新）

**修复产品页表头白字不可见：**
- 根因：`.page-content table th` 设为深色背景白色文字，覆盖了产品页 `.spec-table th` 的浅灰底样式
- 修复：`.page-content table th` 回退为浅灰底深字；新增 `.page-content .spec-data-table thead th` 保留深色表头
- 已推送（commit: 60c81fc）

## 2025-06-07 上午（最新）

**MD文章排版优化 + 导航宽度自适应：**
- `_layouts/default.html`：`{{ content }}` 加 `<article class="page-content">` 包裹，使CSS选择器 `.page-content` 正确作用
- `assets/css/style.css`：重写 `.page-content` 完整排版（h1蓝色2px粗下边线、h2浅灰下划线、表格深色表头、代码红色高亮、blockquote灰底、列表间距等）
- 导航下拉菜单：`min-width: 200px` 改为 `width: max-content; min-width: 110~120px`，消除多余空白
- 所有简短 MD 文件（inverter/servo-drive/solar-inverter/ups/welder 及 frequency-converter/medical-equipment/ups-systems/welding-equipment）补充了描述段落和"选型要点"
- 已推送（commit: 2fdcad3）

## 2025-06-07 上午（再续）

**导航动态化 + MD文件显示修复：**
- `header.html` 完全重写：应用领域、技术资料改为用 Jekyll `site.pages` 动态扫描，新增方案自动出现在菜单
- 所有应用领域/技术资料 md 文件加了 `nav_order` 字段控制菜单排序
- 所有 md 文件 title 简化（去掉"电容方案"后缀），菜单显示更简洁
- 修复 `guidelines.md` 中的 LaTeX 数学公式（改为 HTML formula-block）
- 修复 `terminal-dimensions.md` 和 `part-number-system.md` 中的 `{% include image_placeholder %}` 标签（改为 HTML 注释）
- 新增 `.formula-block` CSS 样式（左边框 + 等宽字体）
- 已推送

## 2025-06-07 上午（续）

**修复应用领域链接打不开问题：**
- 根因：导航链接指向 `/applications/by-industry/` 和 `/applications/by-equipment/`，但目录下缺少 `index.md`
- 修复：新建 `applications/by-industry/index.md` 和 `applications/by-equipment/index.md` 两个汇总页
- 附加：为导航栏"产品中心"、"应用领域"、"技术资料"一级菜单添加链接，已推送

## 2025-06-07 — 页面紧凑化与面包屑优化

**修改内容**：
- `assets/css/style.css` 全面重写，整体风格更紧凑、数据展示导向
- 面包屑：增加 `background: var(--c-bg-alt)` + `border-bottom: 1px solid var(--c-border)`，与内容区形成清晰分隔过渡
- 主内容区：`padding-bottom` 从 60px 减至 40px
- 标题间距：`h2` 顶部 margin 从 1.8em 减至 1.2em，`h3` 从 1.5em 减至 1.0em
- 段落/列表间距：统一收紧（p 0.6em，li 0.15em）
- 产品页：header margin 从 30px 减至 16px，section margin 从 30px 减至 18px
- 表格：cell padding 从 8px 减至 5-6px，字体保持 0.8125rem
- 新增 Markdown 内容表格统一样式（`main > article table` 等选择器），确保 `{{ content }}` 里的表格与参数表风格一致
- 移动端同步收紧适配

**用户偏好记录**：
- 网站风格要求：紧凑、专业、不花哨，参数展示型
- Logo 只显示 HUAYU（无中文）
- 页脚极简：仅公司名 + 邮箱，高度与页头对齐（50px）

---

## 2025-06-07 下午 — PDF转MD + 内容丰富 + SEO优化

**完成工作（commit: ebe581b）**：

### 1. 产品页批量生成（17个系列）
- 使用 pdfplumber 提取 PDF 规格书内容，生成完整产品 MD 文件
- 牛角型：CD292/CD293/CD294/CD295/CD296/CD297/CD298/CD17FX（8个）
- 螺栓型：CD135/CD136/CD13N/CD13NH/CD13L/CD13HL/CD92/CD92L/CD98SK（9个）
- 每个产品页包含：产品介绍、详细规格参数表、尺寸规格、应用场景、选型指南
- 添加了 SEO 关键词和详细描述

### 2. 首页丰富
- 新增公司简介、产品优势、应用领域、品质保障等完整板块
- 加入 SEO 关键词：铝电解电容、电解电容器厂家、snap-in电容、screw电容等
- 新增客户案例展示

### 3. 应用领域文章全面丰富（12篇文章）
- 按行业（6篇）：工业电源、新能源、变频器、医疗设备、UPS、电焊设备
- 按设备（5篇）：逆变器、伺服驱动器、光伏逆变器、UPS、电焊机
- 每篇文章包含：核心要求、电容配置方案、选型计算、推荐产品、保养建议
- 添加了详细的表格、计算公式、选型要点

### 4. SEO优化
- 所有文章加入相关关键词和长尾词
- 产品页包含详细参数表，利于搜索引擎收录
- 应用场景文章包含实际选型建议，提高页面价值
- 关键词覆盖：铝电解电容、电解电容器、snap-in、screw、DC-Link、纹波电流等

**文件统计**：30个文件变更，新增7784行内容
